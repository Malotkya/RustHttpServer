[package]
name = "http_server"
version = "0.1.0"
edition = "2024"

[dependencies]
http_core = { path="../http_core" }
http_macro = { path="../http_macro" }
async_lib = { path ="../async_lib" }
regex = "1.12.3"
