# Html Library Macro

## create_element!

Creats a representation of an html element that can then be rendered.

## ts_function! & js_function!

An attempt at converting rust tokens into a struct that represents a Javascript function.